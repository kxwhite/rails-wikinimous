<div class="index-container">
  <ul>
    <% @articles.each do |a| %>
      <li>
        <h2><%= link_to a.title, article_path(a) %></h2>
        <p><%= a.content %></p>
      </li>
    <% end %>
  </ul>
</div>
